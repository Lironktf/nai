# ============================================================
# TrustHandshake — Environment Variables
# Copy to .env and fill in all values before running.
# NEVER commit .env to version control.
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
# Found in: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key   # server only — never expose to client
SUPABASE_ANON_KEY=your-anon-key                   # safe for client-side use

# ── JWT ──────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-256-bit-hex-secret
JWT_EXPIRY=900    # seconds (15 minutes)

# ── AWS S3 (enrollment video storage) ───────────────────────
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=trusthandshake-enrollment-videos
S3_PRESIGN_EXPIRY_SECONDS=900   # 15-minute pre-signed URL expiry

# ── Persona KYC ──────────────────────────────────────────────
PERSONA_API_KEY=your-persona-api-key
PERSONA_WEBHOOK_SECRET=your-persona-webhook-signing-secret
PERSONA_TEMPLATE_ID=your-inquiry-template-id

# ── Qdrant (face embedding vector store) ─────────────────────
# Local: docker run -p 6333:6333 qdrant/qdrant
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=                   # leave empty for local; set for Qdrant Cloud

# ── Azure Face API (face embedding + liveness) ────────────────
# https://portal.azure.com → Create resource → Face API (free tier: 30k/month)
AZURE_FACE_ENDPOINT=https://your-resource.cognitiveservices.azure.com
AZURE_FACE_KEY=your-azure-face-api-key

# ── Mobile ────────────────────────────────────────────────────
EXPO_PUBLIC_API_URL=http://localhost:3001

# ── Server ───────────────────────────────────────────────────
PORT=3001
CLIENT_URL=http://localhost:5173
NODE_ENV=development
